<%- include('partials/header') %>

<main>
    <section class="aulas-lista">
        <div class="container">
            <h1>Veja nossas aulas</h1>

            <% aulas.forEach(aula=>{ %>
            <article>
                <% if (aula.image) { %>
                <img src=" /images/<%= aula.image %>" alt=""> 
                <% } else { %> 
                <div class="thumb"></div>
                <% } %>
                <div class="info">
                    <h2><%= aula.nome %> </h2>
                    <p><%= aula.valor %> </p>
                    <p><%= aula.descricao %> </p>
                    <a href="#" class="btn btn-primary">Escolher</a>
                </div>
            </article>
            <% }) %>
        </div>
    </section>
</main>

<%- include('partials/footer') %>